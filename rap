#!/usr/bin/env bash
# RetroArch Toolkit launcher script

# Get the directory where this script is located
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Run the main Python script
python3 "$SCRIPT_DIR/main.py" "$@"
